<nav class="navbar navbar-expand-lg bg-light"></nav>
  <div class="container">
    <a class="navbar-brand fw-bold fs-5" href="/visual_sphere">VisualSphere</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/visual_sphere">Connection</a>
        </li>
        <!--li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/visual_sphere/db_connections/new">Connect</a>
        </li -->
      </ul>
      <% if current_user.present? %>
        <li class="dropdown d-flex">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user&.email %>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Logout', logout_path, method: :delete, class: "dropdown-item" %></li>
          </ul>
        </li>
      <% end %>
    </div>
  </div>
</nav>